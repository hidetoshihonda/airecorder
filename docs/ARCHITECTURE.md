# AI Voice Recorder - システムアーキテクチャ解説

> このドキュメントは、AI Voice Recorderの技術的な仕組みを理解したい方向けに、システム全体のアーキテクチャ、使用しているAzureサービス、およびデータフローを解説します。

---

## 🔰 はじめに：「このアプリって何？」をざっくり理解する

> **プログラミングに詳しくない方へ** — 細かい技術用語が出てくる前に、全体像をつかみましょう！

### 一言で言うと

**「話すだけで、文字起こし・翻訳・議事録まで全部やってくれるWebアプリ」** です。

ZoomやTeamsの録画とは違い、ブラウザを開いてボタンを押すだけで使えます。アプリのインストールも不要です。

### 何が起きているの？（ざっくり版）

```
🎤 あなたが話す
   ↓
📱 ブラウザが音声をキャッチ
   ↓
☁️ Azureのサーバーに音声を送信（インターネット経由）
   ↓
📝 AIが「音声→文字」に変換して返してくれる（0.数秒で！）
   ↓
🌐 別のAIが翻訳してくれる（オプション）
   ↓
💾 終わったらデータベースに保存
   ↓
🤖 GPT-4が議事録を自動作成（ボタン一つ！）
```

### よく出てくる用語の早見表

| 用語 | ざっくり説明 |
|------|-------------|
| **Azure** | Microsoftが提供するクラウドサービスの総称。サーバーやAIを「借りて使える」仕組み |
| **API** | アプリ同士がデータをやり取りするための「窓口」のようなもの |
| **フロントエンド** | ユーザーが直接触る画面部分（ブラウザで見える部分） |
| **バックエンド** | 画面の裏側で動いている処理（データベース操作やAIの呼び出しなど） |
| **サーバーレス** | 自分でサーバーを管理しなくて良い仕組み。使った分だけ課金される |
| **WebSocket** | ブラウザとサーバーが「常につながった状態」で通信する方法。LINEの「入力中…」みたいなリアルタイム通信に使う |
| **OAuth** | 「GitHubアカウントでログイン」のような、他のサービスのアカウントを使った認証の仕組み |
| **SDK** | 開発キット。難しい処理を簡単に呼び出せるようにまとめたライブラリ |
| **CDN** | コンテンツ配信ネットワーク。世界中のサーバーにコピーを置いて、ユーザーに近い場所から高速配信する仕組み |
| **NoSQL** | テーブル（表形式）ではなく、JSON（{}の形式）でデータを保存するデータベースの種類 |
| **パーティション** | データを分割して保存する仕組み。ユーザーごとにデータを分けて高速にアクセスできるようにする |

---

## 📌 概要

AI Voice Recorderは、音声をリアルタイムで文字起こしし、翻訳・要約まで行うWebアプリケーションです。すべてMicrosoft Azureのサービスを活用して構築されています。

### 主な機能
- 🎤 リアルタイム音声認識（文字起こし）
- 🗣️ 話者識別（誰が話しているか自動判別）
- 🌐 リアルタイム翻訳（日本語↔英語など）
- 📝 AI議事録生成（GPT-4による要約）
- 💾 録音データの保存・履歴管理

---

## 🏗️ システムアーキテクチャ図

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              ユーザーのブラウザ                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │  Next.js (React) フロントエンド                                         ││
│  │  ├─ 音声キャプチャ (MediaRecorder API)                                  ││
│  │  ├─ Speech SDK (WebSocket接続)                                          ││
│  │  └─ UI/UX コンポーネント                                                ││
│  └─────────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────────┘
                │                    │                    │
                ▼                    ▼                    ▼
┌───────────────────────┐ ┌──────────────────┐ ┌─────────────────────────────┐
│  Azure Speech Services │ │ Azure Translator │ │  Azure Static Web Apps      │
│  (リアルタイム音声認識) │ │  (テキスト翻訳)   │ │  (静的ファイルホスティング)  │
│  ├─ Speech-to-Text    │ │                  │ │  ├─ GitHub認証              │
│  └─ Speaker Diarization│ │                  │ │  └─ CDN配信                 │
└───────────────────────┘ └──────────────────┘ └─────────────────────────────┘
                                                          │
                                                          │ BYOF (Linked Backend)
                                                          ▼
                                               ┌──────────────────────────────┐
                                               │  Azure Functions (API)       │
                                               │  ├─ 録音データCRUD           │
                                               │  ├─ 議事録生成API            │
                                               │  └─ Blob SAS URL生成         │
                                               └──────────────────────────────┘
                                                          │
                        ┌─────────────────────────────────┼─────────────────────┐
                        ▼                                 ▼                     ▼
             ┌────────────────────┐          ┌─────────────────┐    ┌─────────────────┐
             │  Azure Cosmos DB   │          │  Azure Blob     │    │  Azure OpenAI   │
             │  (録音メタデータ)   │          │  Storage        │    │  (GPT-4)        │
             │  └─ userId分離     │          │  (音声ファイル)  │    │  (議事録生成)    │
             └────────────────────┘          └─────────────────┘    └─────────────────┘
```

> 💡 **レストランに例えると…**
>
> | アプリの要素 | レストランで言うと |
> |------------|------------------|
> | **ブラウザ（フロントエンド）** | お客さんが座るテーブルとメニュー表 |
> | **Azure Static Web Apps** | レストランの建物（お店そのもの） |
> | **Azure Functions（API）** | キッチンの料理人（注文を受けて処理する） |
> | **Azure Cosmos DB** | レシピ帳や注文台帳（データの記録） |
> | **Azure Blob Storage** | 冷蔵庫（大きなファイルを保管する場所） |
> | **Azure Speech Services** | 通訳スタッフ（音声を文字にしてくれる） |
> | **Azure OpenAI (GPT-4)** | 超優秀な秘書（議事録をまとめてくれる） |
> | **GitHub認証** | 会員カードでの本人確認 |

---

## 🔧 使用技術スタック

### フロントエンド

| 技術 | バージョン | 用途 |
|------|-----------|------|
| **Next.js** | 16.1.6 | Reactフレームワーク（App Router, Static Export） |
| **React** | 19.2.3 | UIライブラリ |
| **TypeScript** | 5.x | 型安全な開発 |
| **Tailwind CSS** | 4.x | スタイリング |
| **Radix UI** | 各種 | アクセシブルなUIコンポーネント |
| **Speech SDK** | 1.47.0 | Azure Speech ServicesへのWebSocket接続 |

> 💡 **初心者向け解説：フロントエンドの技術選定**
>
> - **React** … Webページを「部品（コンポーネント）」の組み合わせで作るためのライブラリ。ボタン、ヘッダー、録音画面など、それぞれをパーツとして作り、レゴブロックのように組み合わせます
> - **Next.js** … Reactだけだと大変な設定（ページ遷移、ビルド最適化など）を自動でやってくれる「Reactの便利キット」
> - **TypeScript** … JavaScriptに「型（変数の種類指定）」を追加した言語。「この変数は文字列のはずなのに数字が入った！」みたいなバグを事前に防げます
> - **Tailwind CSS** … HTMLに直接 `class="bg-blue-500 text-white"` のように書くだけでデザインできるCSSフレームワーク
> - **Static Export** … 画面をあらかじめHTMLファイルとして書き出しておく方式。サーバーなしで動くので安くて速い！

### バックエンド

| 技術 | バージョン | 用途 |
|------|-----------|------|
| **Azure Functions** | v4 | サーバーレスAPI |
| **Node.js** | 20 | ランタイム |
| **TypeScript** | 5.7 | 型安全な開発 |

> 💡 **初心者向け解説：バックエンドの仕組み**
>
> - **Azure Functions** … 「誰かがAPIを呼んだときだけ起動する」仕組み。普段はサーバーが寝ていて、リクエストが来たら起きて処理して、終わったらまた寝ます。だから使わない時間はお金がかかりません（従量課金）
> - **Node.js** … JavaScriptをサーバーサイドで動かすための環境。フロントエンドもバックエンドも同じ言語（TypeScript/JavaScript）で書けるので、開発効率が良い
> - このアプリでは「録音データの保存・取得」「議事録の生成」「音声ファイルのアップロードURL発行」などの処理をAPIとして提供しています

### データストア

| サービス | 用途 | 特徴 |
|----------|------|------|
| **Azure Cosmos DB** | 録音メタデータ保存 | NoSQL、userId でパーティション分割 |
| **Azure Blob Storage** | 音声ファイル保存 | オブジェクトストレージ |

> 💡 **初心者向け解説：データの保存先が2つある理由**
>
> データの性質に応じて最適な保存先を使い分けています：
> - **Cosmos DB（メタデータ用）** … 録音のタイトル、文字起こしテキスト、話者情報など「検索・更新が頻繁なデータ」を保存。Excelの表のように、特定の条件でデータを素早く検索できます
> - **Blob Storage（音声ファイル用）** … 録音した音声ファイル（WAV/WebMなど）は数MB〜数百MBと大きいので、大容量ファイル専用の安価なストレージに保存します。Google Driveのファイル保管と似たイメージです
>
> 📦 1つにまとめないのは、**コストと速度の最適化**のためです。大きな音声ファイルをデータベースに入れると、検索が遅くなり、料金も高くなります。

### AI/Cognitiveサービス

| サービス | 用途 | 料金体系 |
|----------|------|---------|
| **Azure Speech Services** | 音声認識、話者識別 | S0従量課金（約$1/時間） |
| **Azure Translator** | テキスト翻訳 | 従量課金（約$10/100万文字） |
| **Azure OpenAI (GPT-4)** | 議事録自動生成 | 従量課金 |

> 💡 **初心者向け解説：AIサービスを「自分で作らない」理由**
>
> 音声認識やAI翻訳を一から作るのは、何年もかかる超大規模プロジェクトです。このアプリでは、Microsoftが作って公開している**学習済みのAIサービスを「API経由で借りて使う」** というアプローチを取っています。
>
> イメージとしては：
> - ❌ 自分で翻訳エンジンを開発する（数年＋数億円）
> - ✅ Microsoftの翻訳サービスに「この文を訳して」とリクエストする（数秒＋数円）
>
> これが **クラウドAIの最大のメリット** です。

### ホスティング/認証

| サービス | 用途 |
|----------|------|
| **Azure Static Web Apps** | フロントエンドホスティング、認証、CDN |
| **GitHub OAuth** | ユーザー認証（SWA組み込み認証） |

> 💡 **初心者向け解説：なぜGitHubでログイン？**
>
> 一般的なWebアプリでは「メールアドレス＋パスワード」で会員登録しますが、これには以下の課題があります：
> - パスワードの安全な管理が大変（漏洩リスク）
> - ユーザー登録フォームの開発が必要
> - パスワードリセット機能なども作る必要がある
>
> そこでこのアプリでは **GitHub OAuth**（「GitHubアカウントでログイン」）を採用。GitHubが本人確認を代行してくれるので、パスワードを一切管理する必要がありません。このアプリのターゲットユーザーはエンジニアが中心なので、GitHubアカウントは全員持っている前提です。

---

## 🔄 データフロー詳細

### 1. 音声認識フロー

> 🔰 **やさしく言うと:** マイクで拾った声をインターネット経由でAzureのAIに送ると、AIが「今こう言いましたよね？」とテキストを返してくれます。しかも話している途中からリアルタイムで！YouTubeの自動字幕と似た仕組みですが、自分のアプリに組み込めるのがポイントです。

```
[マイク] → [ブラウザ] → [WebSocket] → [Azure Speech Services]
                                              │
              ← ← ← 中間結果（リアルタイム） ← ←│
              ← ← ← 確定結果 + 話者ID ← ← ← ← │
```

**技術的な詳細:**

1. **音声キャプチャ**: ブラウザの`MediaRecorder API`でマイク音声を取得
2. **WebSocket接続**: Speech SDKが自動的にAzure Speech Servicesとの常時接続を管理
3. **ストリーミング認識**: 音声データは小さなチャンク（100ms程度）で送信
4. **中間結果**: 認識途中のテキストがリアルタイムで返却（灰色で表示）
5. **確定結果**: 文が完結すると確定テキストとして返却
6. **話者識別**: `ConversationTranscriber`を使用すると、各発話に`speakerId`が付与

**コード例（useSpeechRecognition.ts より抜粋）:**
```typescript
// Speech SDKの設定
const speechConfig = SpeechSDK.SpeechConfig.fromSubscription(subscriptionKey, region);
speechConfig.speechRecognitionLanguage = language;

// 話者識別を有効にした場合
const transcriber = new SpeechSDK.ConversationTranscriber(speechConfig, audioConfig);

// 確定結果のコールバック
transcriber.transcribed = (sender, event) => {
  const text = event.result.text;
  const speakerId = event.result.speakerId; // "Guest-1", "Guest-2" など
  // UIに反映
};
```

### 2. リアルタイム翻訳フロー

> 🔰 **やさしく言うと:** 音声認識で文字になったテキストを、Google翻訳のような翻訳サービスに投げて結果をもらいます。1文あたり0.1〜0.2秒で返ってくるので、ほぼリアルタイムに翻訳結果が画面に表示されます。

```
[確定テキスト] → [Azure Translator REST API] → [翻訳結果]
     │                                              │
     └──────────── 100-200ms 程度 ────────────────→ │
```

**技術的な詳細:**

1. **トリガー**: 音声認識で文が確定するたびに翻訳APIを呼び出し
2. **REST API**: `POST https://api.cognitive.microsofttranslator.com/translate`
3. **言語コード変換**: `ja-JP` → `ja`、`en-US` → `en` に変換して送信
4. **認証**: `Ocp-Apim-Subscription-Key` ヘッダーでAPIキーを送信

**コード例（useTranslation.ts より抜粋）:**
```typescript
const response = await fetch(
  `${endpoint}/translate?api-version=3.0&from=${fromLang}&to=${toLang}`,
  {
    method: "POST",
    headers: {
      "Ocp-Apim-Subscription-Key": subscriptionKey,
      "Ocp-Apim-Subscription-Region": region,
      "Content-Type": "application/json",
    },
    body: JSON.stringify([{ text }]),
  }
);
```

### 3. 議事録生成フロー

> 🔰 **やさしく言うと:** ChatGPTに「この会議の文字起こしから議事録を作って」とお願いするのと同じことを、プログラムから自動的にやっています。ただし、毎回同じフォーマット（概要・重要ポイント・アクションアイテム）で出力されるよう、あらかじめ「こういう形式で返してね」という指示（プロンプト）を仕込んでいます。

```
[全文テキスト] → [Azure Functions] → [Azure OpenAI] → [構造化された議事録]
```

**技術的な詳細:**

1. **入力**: 音声認識で得られた全文テキスト
2. **プロンプトエンジニアリング**: 会議タイプ別のテンプレート（定例会議、1on1、商談など）
3. **出力形式**: JSON形式で構造化（概要、重要ポイント、アクションアイテム）
4. **言語対応**: プロンプトで出力言語を指定可能

**プロンプト例:**
```
あなたは会議の議事録を作成する専門家です。
与えられた文字起こしテキストから、構造化された議事録を作成してください。

出力は必ず以下のJSON形式で返してください：
{
  "overview": "会議の概要（2-3文で簡潔に）",
  "keyPoints": ["重要ポイント1", "重要ポイント2", ...],
  "actionItems": [
    {
      "id": "1",
      "description": "アクションアイテムの説明",
      "assignee": "担当者名",
      "dueDate": "期限"
    }
  ]
}
```

### 4. データ保存フロー

> 🔰 **やさしく言うと:** 録音が終わると、「何を話したか」のテキスト情報はCosmos DBに、「音声ファイルそのもの」はBlob Storageに、それぞれ最適な場所に保存されます。次回アプリを開いたとき、履歴画面で過去の録音を見たり、音声を再生したりできます。

```
[録音終了] → [メタデータ] → [Cosmos DB]
          → [音声ファイル] → [Blob Storage]
```

**Cosmos DB スキーマ:**
```typescript
interface Recording {
  id: string;              // 一意のID (UUID)
  userId: string;          // パーティションキー（ユーザー分離）
  title: string;           // 録音タイトル
  transcript: string;      // 文字起こし全文
  segments: Segment[];     // 話者別セグメント
  sourceLanguage: string;  // 元の言語
  summary?: Summary;       // AI生成議事録
  audioBlobUrl?: string;   // 音声ファイルURL
  createdAt: string;       // 作成日時
  updatedAt: string;       // 更新日時
}
```

---

## 🔐 認証・認可アーキテクチャ

> 🔰 **やさしく言うと:**
>
> 認証とは「あなたは誰？」を確認すること、認可とは「あなたはこの操作をしていいの？」を確認することです。
>
> このアプリでは、ログインしていない人は録音機能を使えないようにしています。また、Aさんの録音データをBさんが見たり消したりできないようにしています。
>
> **具体的なログインの流れ：**
> 1. 録音ページを開こうとする → 「ログインしてください」と表示される
> 2. 「GitHubでログイン」ボタンを押す
> 3. GitHubのログイン画面が出る → IDとパスワードを入力
> 4. 「このアプリにアカウント情報を渡していい？」と聞かれる → 許可する
> 5. アプリに戻ってきて、ログイン済み状態になる
>
> これ以降は、ブラウザが自動的に「この人はログイン済みです」という情報（Cookie）を送るので、毎回ログインし直す必要はありません。

### Azure Static Web Apps 組み込み認証

```
[ブラウザ] → [SWA] → [/.auth/login/github] → [GitHub OAuth]
                                                   │
              ← ← ← 認証Cookie発行 ← ← ← ← ← ← ← ←│
```

**フロー:**

1. ユーザーが保護ページ（/recording, /history）にアクセス
2. SWAのルート設定により、未認証の場合は401を返す
3. 401の場合、`/.auth/login/github`にリダイレクト
4. GitHubでOAuth認証
5. 認証成功後、SWAが認証Cookieを発行
6. 以降のリクエストにCookieが自動付与

**API認証（BYOF - Bring Your Own Functions）:**

> 🔰 **BYOFって何？** — Static Web Appsに「自分で用意したAPI（Azure Functions）をつなぐ」機能です。これを使うと、SWAが自動的に「この人はログイン済みのユーザーAです」という情報をAPIに伝えてくれます。API側では、その情報を見て「このリクエストは信頼できる」と判断します。

```
[ブラウザ] → [SWA] → [x-ms-client-principal ヘッダー付与] → [Azure Functions]
                                                                │
                                                   ヘッダーをBase64デコード
                                                                │
                                                   { userId, userDetails, userRoles }
```

**認証ヘルパー（api/src/utils/auth.ts より）:**
```typescript
export function getClientPrincipal(request: HttpRequest): ClientPrincipal | null {
  const header = request.headers.get("x-ms-client-principal");
  if (!header) return null;

  const decoded = Buffer.from(header, "base64").toString("utf-8");
  return JSON.parse(decoded);
}
```

---

## 🌐 デプロイメント構成

> 🔰 **デプロイメントとは？** — 開発したアプリを「インターネット上に公開して誰でも使える状態にする」ことです。ローカル（自分のPC）で動いているだけでは自分しか使えないので、Azureのサーバーにアップロードして公開します。

### 環境

| 環境 | URL | 用途 |
|------|-----|------|
| Production | `proud-rock-06bba6200.2.azurestaticapps.net` | 本番環境 |
| Local | `localhost:3000` | 開発環境 |

### Azureリソース一覧

| リソース名 | 種類 | リージョン | SKU |
|-----------|------|-----------|-----|
| `rg-airecorder-dev` | リソースグループ | Japan East | - |
| `swa-airecorder-dev` | Static Web Apps | East Asia | Standard |
| `func-airecorder-dev` | Functions App | Japan East | Consumption |
| `cosmos-airecorder-dev` | Cosmos DB | Japan East | Serverless |
| `st-airecorder-dev` | Storage Account | Japan East | Standard LRS |
| `speech-airecorder-dev` | Speech Services | Japan East | S0 |
| `translator-airecorder-dev` | Translator | Global | S1 |
| `openai-airecorder-dev` | OpenAI | East US | S0 |

### CI/CD

現在は手動デプロイ：

```powershell
# API デプロイ
cd api
npm run build
func azure functionapp publish func-airecorder-dev

# Web デプロイ  
cd web
npm run build
swa deploy ./out --env production --app-name swa-airecorder-dev
```

---

## 📊 コスト構造

> 🔰 **なぜお金がかかるの？** — このアプリは自分のパソコンではなく、Microsoftのクラウド（Azure）上で動いています。クラウドは「使った分だけ料金を払う」仕組みなので、たくさん使えば高くなり、使わなければ安くなります。自分でサーバーを買って維持するよりもずっと安く、手軽に運用できます。

### 月額概算（中程度の利用を想定）

| サービス | 無料枠 | 超過時料金 | 概算コスト |
|----------|--------|-----------|-----------|
| Static Web Apps | Standard: $9/月 | - | $9 |
| Functions | 100万回/月無料 | $0.20/100万回 | ~$0 |
| Cosmos DB | 1000 RU/s無料 | $0.008/RU/時間 | ~$5 |
| Blob Storage | 5GB無料 | $0.02/GB | ~$1 |
| Speech Services | なし | $1/時間 | $10-50 |
| Translator | 200万文字/月無料 | $10/100万文字 | ~$5 |
| OpenAI (GPT-4) | なし | 入力$30, 出力$60/100万トークン | $5-20 |
| **合計** | | | **$35-100/月** |

※ 利用頻度により大きく変動

> 💡 **具体的にどのくらい？**
>
> - 1日30分の会議を週5回録音 → 月約10時間 → Speech費用 約$10
> - 各会議で議事録生成 → 月約20回 → OpenAI費用 約$5
> - **個人利用なら月$35〜50程度** が目安です
> - 全く使わない月でも Static Web Apps の固定費 $9 はかかります

---

## 🔍 トラブルシューティング

### よくある問題

| 症状 | 原因 | 対処 |
|------|------|------|
| 「Quota exceeded」エラー | Speech Services無料枠超過 | S0にアップグレード |
| 音声認識が開始しない | マイク権限未許可 | ブラウザ設定確認 |
| 翻訳が動作しない | APIキー未設定 | 環境変数確認 |
| 認証後も401エラー | BYOF設定漏れ | `az staticwebapp backends link`実行 |

### ログ確認方法

```powershell
# Functions のログ
func azure functionapp logstream func-airecorder-dev

# アプリケーション Insights（設定済みの場合）
az monitor app-insights query --app <app-insights-name> --analytics-query "traces | take 100"
```

---

## 📚 参考リンク

- [Azure Speech Services ドキュメント](https://learn.microsoft.com/azure/cognitive-services/speech-service/)
- [Azure Static Web Apps 認証](https://learn.microsoft.com/azure/static-web-apps/authentication-authorization)
- [Azure Functions Node.js 開発ガイド](https://learn.microsoft.com/azure/azure-functions/functions-reference-node)
- [Next.js Static Export](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)
- [Cosmos DB パーティション設計](https://learn.microsoft.com/azure/cosmos-db/partitioning-overview)

---

## 📝 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-02-08 | 初版作成 |
