# ===========================================
# AI Recorder 環境変数設定
# ===========================================
# このファイルを .env.local にコピーして値を設定してください
# cp .env.example .env.local
#
# 【重要】各値の取得方法は README.md を参照
# ===========================================

# -------------------------------------------
# Azure Speech Services (文字起こし用)
# -------------------------------------------
# 取得: az cognitiveservices account keys list --name speech-airecorder-dev --resource-group rg-airecorder-dev
NEXT_PUBLIC_AZURE_SPEECH_KEY=your-speech-key-here
# リージョン: az cognitiveservices account show --name speech-airecorder-dev --resource-group rg-airecorder-dev --query location
NEXT_PUBLIC_AZURE_SPEECH_REGION=japaneast

# -------------------------------------------
# Azure Translator (翻訳用)
# -------------------------------------------
# 取得: az cognitiveservices account keys list --name translator-airecorder-dev --resource-group rg-airecorder-dev
NEXT_PUBLIC_AZURE_TRANSLATOR_KEY=your-translator-key-here
# 【注意】Translatorは通常 "global" リージョン
# リージョン: az cognitiveservices account show --name translator-airecorder-dev --resource-group rg-airecorder-dev --query location
NEXT_PUBLIC_AZURE_TRANSLATOR_REGION=global

# -------------------------------------------
# API URL (Azure Functions)
# -------------------------------------------
NEXT_PUBLIC_API_URL=https://func-airecorder-dev.azurewebsites.net/api

# -------------------------------------------
# Azure AD B2C (認証用 - オプション)
# -------------------------------------------
# NEXT_PUBLIC_AZURE_AD_B2C_CLIENT_ID=
# NEXT_PUBLIC_AZURE_AD_B2C_TENANT_NAME=
# NEXT_PUBLIC_AZURE_AD_B2C_POLICY_NAME=
